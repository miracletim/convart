class Videos {
  /**
   * Find cached videos by region code
   * @param {Object} reqData - Request Data (regionCode)
   * @returns {Object} Cached videos ()
   */
  static async findCachedVideos({ regionCode }) {
    // TODO
    return result;
  }
}
